            <hostname>{{ server.hostname }}</hostname>
            <port>{{ server.port }}</port>
            <socketType>{{ server.socket }}</socketType>
            <authentication>{{ server.auth }}</authentication>
            <username>{{ user.userName }}</username>
